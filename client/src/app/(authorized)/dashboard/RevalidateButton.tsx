'use client';

import { Button } from 'src/components/ui/shadcn-ui/button';

const RevalidateButton = () => {
  return <Button onClick={() => {}}>Revalidate users</Button>;
};

export default RevalidateButton;
